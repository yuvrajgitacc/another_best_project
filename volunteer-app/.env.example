# Backend API base URL — required for APK builds (Capacitor runs from file:// so relative URLs break)
# Set this to your deployed backend URL before running: npm run build && npx cap sync android
VITE_API_BASE_URL=https://your-backend.onrender.com
